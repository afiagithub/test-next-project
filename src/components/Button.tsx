'use client'
import React from 'react'

const Button = () => {
    return (
        <button className="btn btn-primary" onClick={() => alert("hello")}>
            Click me
        </button>
    )
}

export default Button
